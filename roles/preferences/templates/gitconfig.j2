[user]
  name = {{ git_user_name}}
  email = {{ git_user_email }}

[core]
  editor = vim
  pager = less -R

[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
